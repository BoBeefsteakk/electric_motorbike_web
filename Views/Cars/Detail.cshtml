@using System.Text.Json
@model VinfastWeb.Models.Car
@{
    ViewData["Title"] = Model.Name;

    var colors = new[] {
        new { Name = "Trắng", Hex = "#F5F5F0", Border = "#d1d5db" },
        new { Name = "Đen",   Hex = "#111827", Border = "#111827" },
        new { Name = "Đỏ",    Hex = "#C0392B", Border = "#C0392B" },
        new { Name = "Vàng",  Hex = "#D4A017", Border = "#D4A017" },
    };

    var specs = Model.Category switch
    {
        "dong_co_dien" => new[] { ("500km", "1 lần sạc"), ("180km/h", "Tối đa"), ("EV", "Động cơ điện") },
        "dong_co_xang" => new[] { ("650km", "1 lần đổ đầy"), ("190km/h", "Tối đa"), ("Xăng", "Động cơ xăng") },
        "dong_xe_dich_vu" => new[] { ("700km", "1 lần đổ đầy"), ("160km/h", "Tối đa"), ("Dịch vụ", "Khai thác bền bỉ") },
        _ => new[] { ("550km", "Tầm hoạt động"), ("180km/h", "Tối đa"), ("VinFast", "Chính hãng") }
    };

    var categoryLabel = Model.Category switch
    {
        "dong_co_dien" => "Xe Điện",
        "dong_co_xang" => "Xe Xăng",
        "dong_xe_dich_vu" => "Xe Dịch Vụ",
        _ => "Ô Tô VinFast"
    };
}

<div class="dt-breadcrumb-bar">
    <div class="container">
        <nav class="pd-breadcrumb">
            <a href="/">Trang chủ</a>
            <i class="fas fa-chevron-right"></i>
            <a href="/Cars">Ô Tô</a>
            <i class="fas fa-chevron-right"></i>
            <a href="/Cars?category=@Model.Category">@categoryLabel</a>
            <i class="fas fa-chevron-right"></i>
            <span>@Model.Name</span>
        </nav>
    </div>
</div>

<section class="dt-section">
    <div class="container">
        <div class="dt-layout">

            <div class="dt-left">
                <div class="dt-img-main">
                    <img src="@Model.ImageUrl" alt="@Model.Name" id="mainImg"
                         onerror="this.src='data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 4 3%22%3E%3Crect fill=%22%23f3f4f6%22 width=%224%22 height=%223%22/%3E%3C/svg%3E'" />
                </div>

                <div class="dt-thumbnails">
                    @for (int i = 0; i < 4; i++)
                    {
                        <div class="dt-thumb @(i == 0 ? "active" : "")" onclick="selectThumb(this)">
                            <img src="@Model.ImageUrl" alt="@Model.Name thumb @(i + 1)" />
                        </div>
                    }
                </div>
            </div>

            <div class="dt-right">

                <div class="dt-tag">@categoryLabel</div>

                <h1 class="dt-name">@Model.Name</h1>

                <div class="dt-rating">
                    <div class="dt-stars">
                        <i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i>
                        <i class="fas fa-star"></i><i class="fas fa-star-half-alt"></i>
                    </div>
                    <span class="dt-rating-num">4.8</span>
                    <span class="dt-rating-count">(268 đánh giá)</span>
                    <span class="dt-divider">|</span>
                    <span class="dt-stock"><i class="fas fa-check-circle"></i> Còn hàng</span>
                </div>

                <div class="dt-price-wrap">
                    <div class="dt-price">@Model.FormattedPrice</div>
                    <div class="dt-price-note">Giá niêm yết VinFast · Có hỗ trợ trả góp và lái thử</div>
                </div>

                <p class="dt-desc">
                    @Model.Name là mẫu ô tô VinFast chính hãng với thiết kế hiện đại, vận hành ổn định
                    và phù hợp cho nhu cầu di chuyển gia đình hoặc công việc hằng ngày.
                </p>

                <div class="dt-specs-row">
                    @foreach (var spec in specs)
                    {
                        <div class="dt-spec-box">
                            <div class="dt-spec-val">@spec.Item1</div>
                            <div class="dt-spec-label">@spec.Item2</div>
                        </div>
                    }
                </div>

                <div class="dt-option-group">
                    <div class="dt-option-label">
                        Màu sắc: <strong id="colorName">@colors[0].Name</strong>
                    </div>
                    <div class="dt-color-list">
                        @for (int i = 0; i < colors.Length; i++)
                        {
                            var c = colors[i];
                            <button class="dt-color-btn @(i == 0 ? "active" : "")"
                                    style="background:@c.Hex;border-color:@c.Border"
                                    onclick="selectColor(this, '@c.Name')"
                                    title="@c.Name">
                            </button>
                        }
                    </div>
                </div>

                <div class="dt-option-group">
                    <div class="dt-option-label">Số lượng</div>
                    <div class="dt-qty-wrap">
                        <button class="dt-qty-btn" onclick="changeQty(-1)"><i class="fas fa-minus"></i></button>
                        <span class="dt-qty-val" id="qtyVal">1</span>
                        <button class="dt-qty-btn" onclick="changeQty(1)"><i class="fas fa-plus"></i></button>
                    </div>
                </div>

                <div class="dt-actions">
                    <button class="dt-btn-cart" onclick="addToCartDetail()">
                        <i class="fas fa-shopping-bag"></i> Thêm vào giỏ
                    </button>
                    <button class="dt-btn-buy">
                        <i class="fas fa-tag"></i> Đăng Ký Mua
                    </button>
                    <button class="dt-btn-wish" title="Yêu thích">
                        <i class="far fa-heart"></i>
                    </button>
                </div>

                <div class="dt-badges">
                    <div class="dt-badge"><i class="fas fa-undo"></i> Đổi trả 30 ngày</div>
                    <div class="dt-badge"><i class="fas fa-shield-alt"></i> Bảo hành chính hãng</div>
                    <div class="dt-badge"><i class="fas fa-truck"></i> Hỗ trợ giao xe tận nơi</div>
                </div>

                <div class="dt-info-tabs">
                    <div class="dt-info-tab-header">
                        <button class="dt-itab active" onclick="switchTab(this,'tab-features')">Tính Năng</button>
                        <button class="dt-itab" onclick="switchTab(this,'tab-specs')">Thông Số</button>
                        <button class="dt-itab" onclick="switchTab(this,'tab-warranty')">Bảo Hành</button>
                    </div>

                    <div class="dt-itab-content active" id="tab-features">
                        <div class="dt-accordion">
                            <div class="dt-acc-item open">
                                <div class="dt-acc-head" onclick="toggleAcc(this)">
                                    <span>Ưu Điểm Nổi Bật</span>
                                    <i class="fas fa-minus"></i>
                                </div>
                                <div class="dt-acc-body">
                                    <ul class="dt-feature-list">
                                        <li><i class="fas fa-check"></i> Thiết kế hiện đại, đậm chất VinFast</li>
                                        <li><i class="fas fa-check"></i> Khoang nội thất rộng rãi, tiện nghi</li>
                                        <li><i class="fas fa-check"></i> Hệ thống an toàn và hỗ trợ lái thông minh</li>
                                        <li><i class="fas fa-check"></i> Màn hình trung tâm hiện đại, kết nối đa thiết bị</li>
                                        <li><i class="fas fa-check"></i> Phù hợp cho gia đình và di chuyển đô thị</li>
                                    </ul>
                                </div>
                            </div>

                            <div class="dt-acc-item">
                                <div class="dt-acc-head" onclick="toggleAcc(this)">
                                    <span>Trải Nghiệm Lái Thử</span>
                                    <i class="fas fa-plus"></i>
                                </div>
                                <div class="dt-acc-body">
                                    <p>Khách hàng có thể đăng ký lái thử trực tiếp tại showroom VinFast gần nhất để trải nghiệm thực tế trước khi quyết định mua xe.</p>
                                </div>
                            </div>

                            <div class="dt-acc-item">
                                <div class="dt-acc-head" onclick="toggleAcc(this)">
                                    <span>Chính Sách Giao Xe</span>
                                    <i class="fas fa-plus"></i>
                                </div>
                                <div class="dt-acc-body">
                                    <p>Hỗ trợ giao xe toàn quốc, tư vấn hồ sơ trả góp và thủ tục đăng ký nhanh chóng.</p>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="dt-itab-content" id="tab-specs">
                        <table class="dt-spec-table">
                            <tr><td>Loại xe</td><td>Ô tô</td></tr>
                            <tr><td>Phân khúc</td><td>@categoryLabel</td></tr>
                            <tr><td>Tầm hoạt động</td><td>@specs[0].Item1</td></tr>
                            <tr><td>Tốc độ tối đa</td><td>@specs[1].Item1</td></tr>
                            <tr><td>Động cơ</td><td>@specs[2].Item1 — @specs[2].Item2</td></tr>
                            <tr><td>Hãng</td><td>VinFast</td></tr>
                            <tr><td>Nguồn gốc</td><td>Chính hãng</td></tr>
                            <tr><td>Màu sắc</td><td>4 tùy chọn</td></tr>
                        </table>
                    </div>

                    <div class="dt-itab-content" id="tab-warranty">
                        <div class="dt-warranty-list">
                            <div class="dt-warranty-item">
                                <div class="dt-warranty-icon"><i class="fas fa-shield-alt"></i></div>
                                <div>
                                    <strong>Bảo hành xe chính hãng</strong>
                                    <p>Áp dụng theo chính sách VinFast tại đại lý và showroom toàn quốc.</p>
                                </div>
                            </div>
                            <div class="dt-warranty-item">
                                <div class="dt-warranty-icon"><i class="fas fa-tools"></i></div>
                                <div>
                                    <strong>Hỗ trợ kỹ thuật và bảo dưỡng</strong>
                                    <p>Đặt lịch bảo dưỡng, sửa chữa và kiểm tra định kỳ dễ dàng.</p>
                                </div>
                            </div>
                            <div class="dt-warranty-item">
                                <div class="dt-warranty-icon"><i class="fas fa-headset"></i></div>
                                <div>
                                    <strong>Tư vấn mua xe 24/7</strong>
                                    <p>Hotline 1800 599 961 hỗ trợ tư vấn sản phẩm và thủ tục mua xe.</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </div>
</section>

@section Scripts {
    <script>
        const productId    = @Model.Id;
        const productName  = @Html.Raw(Json.Serialize(Model.Name));
        const productPrice = @Model.Price;
        const productImg   = @Html.Raw(Json.Serialize(Model.ImageUrl));
        let qty            = 1;
        let chosenColor    = @Html.Raw(JsonSerializer.Serialize(colors[0].Name));

        function changeQty(d) {
          qty = Math.max(1, qty + d);
          document.getElementById('qtyVal').textContent = qty;
        }
        function selectColor(btn, name) {
          document.querySelectorAll('.dt-color-btn').forEach(b => b.classList.remove('active'));
          btn.classList.add('active');
          chosenColor = name;
          document.getElementById('colorName').textContent = name;
        }
        function selectThumb(el) {
          document.querySelectorAll('.dt-thumb').forEach(t => t.classList.remove('active'));
          el.classList.add('active');
        }
        function addToCartDetail() {
          addToCart(
            productId,
            'car',
            productName + ' - ' + chosenColor,
            productPrice,
            productImg,
            chosenColor,
            null
          );
        }
        function switchTab(btn, tabId) {
          document.querySelectorAll('.dt-itab').forEach(t => t.classList.remove('active'));
          document.querySelectorAll('.dt-itab-content').forEach(t => t.classList.remove('active'));
          btn.classList.add('active');
          document.getElementById(tabId).classList.add('active');
        }
        function toggleAcc(head) {
          const item = head.closest('.dt-acc-item');
          const isOpen = item.classList.contains('open');
          document.querySelectorAll('.dt-acc-item').forEach(i => {
            i.classList.remove('open');
            i.querySelector('i').className = 'fas fa-plus';
          });
          if (!isOpen) {
            item.classList.add('open');
            head.querySelector('i').className = 'fas fa-minus';
          }
        }
    </script>
}